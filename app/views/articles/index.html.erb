<p><%= link_to "新增文章", new_article_path, :class => "btn btn-primary" %></p>
<table class="table">
<tr>
  <th>標題</th>
  <th>作者</th>
</tr>
<% @articles.each do |t| %>
  <tr>
    <td><%= t.title %></td>
    <td>?</td>
  </tr>
<% end %>
</table>

<%= paginate @articles %>